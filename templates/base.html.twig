<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Admin{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <div class="flex">
        <div class="w-64 h-screen bg-blue-800 text-white">
            <div class="p-4">
                <h2 class="text-2xl font-bold">Admin Panel</h2>
            </div>
            <ul>
                <li class="p-4 hover:bg-blue-700"><a href="{{ path('admin_cours_create') }}">Créer un cours</a></li>
                <li class="p-4 hover:bg-blue-700"><a href="{{ path('admin_niveau_create') }}">Créer un niveau</a></li>
                <li class="p-4 hover:bg-blue-700"><a href="{{ path('admin_cours_list') }}">Liste des cours</a></li>
                <li class="p-4 hover:bg-blue-700"><a href="{{ path('admin_niveaux_list') }}">Liste des niveaux</a></li>
                <li class="p-4 hover:bg-blue-700"><a href="{{ path('app_admin') }}">Tableau de bord</a></li>
            </ul>
        </div>

        <div class="flex-1 p-4">
            {% block content %}{% endblock %}
        </div>
    </div>
</body>
</html>
